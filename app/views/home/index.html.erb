<h1><%= t('pages.welcome') %></h1>
<p>this is home page</p>
<hr/>

<div class="container">
	<h2><%= t('articles.list') %></h2>

		<% @articles.each do |article| %>

			<h3><%= article.title %></h3>
    			<p><%= article.atext %></p>
			<p><%= link_to 'Show full text', article_path(article) %></p>
			<hr/>
		<% end %>
</div>